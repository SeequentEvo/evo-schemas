{
  "$id": "/components/locations/1.0.2/locations.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Locations are coordinates in 3D space. The x, y, z columns follow the axis order of the object's coordinate_reference_system.",
  "type": "object",
  "properties": {
    "coordinates": {
      "$ref": "/elements/float-array-3/1.0.1/float-array-3.schema.json",
      "description": "Coordinates. Columns: x, y, z (the first, second, and third axes of the object's coordinate_reference_system)."
    }
  },
  "required": [
    "coordinates"
  ]
}
