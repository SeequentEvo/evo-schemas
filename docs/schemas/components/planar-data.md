import SchemaUri from '@theme/SchemaUri';
import FlatProperties from '../generated/flatmd/components/planar-data-1.0.1.md';

# planar-data

<SchemaUri uri="schema/components/planar-data/1.0.1/planar-data.schema.json" />

The `planar-data` component associates planar orientation measurements (strike, dip) with spatial locations.
This is used by the [planar-data-pointset](../planar-data-pointset.md) schema.

## Properties

<FlatProperties />
