import SchemaUri from '@theme/SchemaUri';
import FlatProperties from '../generated/flatmd/components/survey-line-1.1.0.md';

# survey-line

<SchemaUri uri="schema/components/survey-line/1.1.0/survey-line.schema.json" />

The `survey-line` component defines a survey line — a spatial path along which geophysical measurements
were collected. It includes location data, timing, and associated survey attributes.

## Properties

<FlatProperties />
