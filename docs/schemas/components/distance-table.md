import SchemaUri from '@theme/SchemaUri';
import FlatProperties from '../generated/flatmd/components/distance-table-1.2.0.md';

# distance-table

<SchemaUri uri="schema/components/distance-table/1.2.0/distance-table.schema.json" />

The `distance-table` component represents a table of distance-based data, associating measured distances
with attribute values.

**See also:** [data-table](data-table.md), [interval-table](interval-table.md).

## Properties

<FlatProperties />
